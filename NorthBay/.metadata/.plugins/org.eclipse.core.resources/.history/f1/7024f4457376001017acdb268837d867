package com.uae.govgpt.testcases;

import java.io.IOException;
import java.util.HashMap;

import org.testng.annotations.Test;

import com.uae.govgpt.abstractcomponents.BaseTest;
import com.uae.govgpt.abstractcomponents.Retry;
import com.uae.govgpt.pageobjects.HomePage;

public class StandAloneTestSample2 extends BaseTest {
	
	
	@Test(dataProvider = "getData")
	public void addItemToCart(HashMap<String, String> map) throws IOException, InterruptedException {
		
		
		login.goTo();
		HomePage productcatalogpage = login.loginToApplication(map.get("email"),map.get("password"));
	}
}
